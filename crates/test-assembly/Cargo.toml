[package]
name = "test-assembly"
version = "0.1.0"
edition = "2021"
publish = false

repository = "https://github.com/madsmtm/objc2"
license = "MIT"

build = "build.rs"

[dependencies]
cargo_metadata = "0.17"
rustc-demangle = "0.1"
regex = "1.6"
lazy_static = "1.4.0"
