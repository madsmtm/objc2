//! # Bindings to the `PhotoKit` framework
//!
//! This actually lives in the `Photos` framework, but `PhotoKit` is the name
//! that people use to refer to it.
#![doc(alias = "PhotoKit")]

pub use crate::generated::Photos::*;
